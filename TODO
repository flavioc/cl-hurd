TODO list:

- find... why?
- shutdown other translators
- fsys set options on other translators
- more callbacks:
  - file-exec -- (broken)
  - file-get-storage-info (must investigate further...)
  - file-get-translator --
  - file-lock-stat (no need for it)
  - file-lock (likewise)
  - file-reparent
  - file-set-translator --
