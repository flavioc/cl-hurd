TODO list for the week:

- Add port class and port bucket to translator class.
- Allocate class and bucket at startup (add method)
- Create *auth-server* (getauth())
- Identity port to translator.
- Server loop (method!).
- Server shutdown (method!)
- Demuxer (3 servers).
- Class to abstract iouser.
- Class to abstract openstat.
- Class to abstract mode_t.
- Class to abstract stat_t.

- transbox?
